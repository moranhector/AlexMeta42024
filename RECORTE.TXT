    <form method="GET" action="{{ route('futurosjubilados') }}">


        <label for="etiqueta">Filtrar por etiqueta:</label>
        <select name="etiqueta" id="etiqueta">
            <option value="" {{ request()->query('etiqueta') == '' ? 'selected' : '' }}>Todos</option>
            <option value="Vencido" {{ request()->query('etiqueta') == 'Vencido' ? 'selected' : '' }}>Vencido</option>
            <option value="Vigente" {{ request()->query('etiqueta') == 'Vigente' ? 'selected' : '' }}>Vigente</option>
        </select>


        <button type="submit">Buscar</button>
    </form>